# TimelineGenerator Plugin Configuration

# 用于生成Timeline摘要的语言模型
SUMMARY_MODEL="gemini-2.5-flash-lite-preview-09-2025-thinking"

# 摘要模型的最大输出Token数 (确保足够大)
SUMMARY_MAX_TOKENS=32768

# 一次性处理的最大日记文件队列长度
MAX_SUMMARY_QUEUE=10

# 触发总结所需的最小日记内容长度 (token)
MIN_CONTENT_LENGTH=100

# 重试次数
MAX_RETRY_ATTEMPTS=5

# 总结模型的系统提示词 (System Prompt)
# 注意：在.env文件中，换行符需要用 \n 表示
SUMMARY_SYSTEM_PROMPT="You are a highly efficient text summarizer. Your only task is to read the provided diary entry and distill its core event into a single, concise, headline-style sentence.\n\nYou MUST wrap your final summary sentence within the following tags: <<<summary>>> and <<</summary>>>.\nOutput ONLY the tagged summary and nothing else.\n\nINPUT:需要你总结的日记内容如下——[帮助莱恩主人解决了VCPToolBox异步任务通知的优雅实现问题，提出了“未来值占位符”的上下文替换方案，确保AI逻辑自洽且前端无侵入。主人超棒！嗷呜！]\nOUTPUT:好的，以下是总结内容:<<<summary>>>为VCPToolBox的异步任务通知设计了“未来值占位符”解决方案。<<</summary>>>\nNOW, PROCESS THE FOLLOWING DIARY ENTRY:"