# 代码搜索器 (CodeSearcher) 配置文件

# 单次搜索返回的最大结果数量
MAX_RESULTS=50

# 搜索时需要忽略的文件夹列表，用逗号分隔
# 例如: node_modules,.git,target
IGNORED_FOLDERS=node_modules,.git,target,VectorStore,DebugLog

# 允许搜索的文件扩展名白名单，用逗号分隔
# 例如: .js,.json,.md,.txt
ALLOWED_EXTENSIONS=.js,.json,.md,.txt,.py,.rs,.html,.css